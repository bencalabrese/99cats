<h1>Hi <%= @user.user_name %></h1>

<h3>Logged in from:</h3>
<ul>
  <% @user.sessions.each do |session| %>
    <li><%= session.session_token %><%= button_to "delete", delete_session_path(session) %></li>
  <% end %>
</ul>
